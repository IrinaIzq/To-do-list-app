# Prometheus configuration for monitoring Flask application

global:
  scrape_interval: 15s # Set the scrape interval to every 15 seconds
  evaluation_interval: 15s # Evaluate rules every 15 seconds
  external_labels:
    monitor: 'todo-app-monitor'

# Alertmanager configuration (optional)
alerting:
  alertmanagers:
    - static_configs:
        - targets: []
          # - alertmanager:9093

# Load rules once and periodically evaluate them
rule_files:
  # - "alerts.yml"

# Scrape configurations
scrape_configs:
  # The Flask application metrics
  - job_name: 'flask-app'
    static_configs:
      - targets: ['app:5000']
    metrics_path: '/metrics'
    scrape_interval: 10s
    
  # Prometheus self-monitoring
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

# Example alert rules (create alerts.yml if needed)
# groups:
#   - name: app_alerts
#     rules:
#       - alert: HighErrorRate
#         expr: rate(flask_http_request_total{status=~"5.."}[5m]) > 0.05
#         for: 5m
#         labels:
#           severity: critical
#         annotations:
#           summary: "High error rate detected"
#           description: "Error rate is {{ $value }} errors per second"
#       
#       - alert: SlowResponseTime
#         expr: flask_http_request_duration_seconds{quantile="0.95"} > 1
#         for: 5m
#         labels:
#           severity: warning
#         annotations:
#           summary: "Slow response time detected"
#           description: "95th percentile response time is {{ $value }} seconds"
#
#       - alert: HighMemoryUsage
#         expr: process_resident_memory_bytes > 500000000
#         for: 5m
#         labels:
#           severity: warning
#         annotations:
#           summary: "High memory usage"
#           description: "Memory usage is {{ $value | humanize }}B"